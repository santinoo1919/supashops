{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm install && npx expo export:web",
        "outputDirectory": "web-build"
      }
    }
  ]
}
